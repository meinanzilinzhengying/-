// =============================================================================
// 向后兼容模块 - 所有 API 逻辑已拆分到 src/api/ 目录下
// 此文件保留原有导出接口，确保现有引用无需修改
// =============================================================================

export { api, createAbortController, resetRedirect } from '../api/index';
